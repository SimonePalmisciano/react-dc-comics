import HeaderNavList from "./HeaderNavlist";

function HeaderNav() {
    return (
        <nav>
            <HeaderNavList/>
        </nav>
    );
}

console.log(HeaderNav());
export default HeaderNav;
